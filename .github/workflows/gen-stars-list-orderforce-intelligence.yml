name: Gen Stars List - OrderForce-Intelligence

run-name: Gen Stars List - OrderForce-Intelligence

on:
  schedule:
    - cron: "0 6 * * 1,3,5" # Run every monday, wednesday and friday at 6:00 AM
  workflow_dispatch: # Manual dispatch

permissions:
  contents: write

env:
  GH_TOKEN: ${{ secrets.GH_PAT }}

defaults:
  run:
    shell: bash
    working-directory: ./scripts

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  gen-stars-list:
    name: Gen Stars List
    runs-on: [self-hosted, Linux, ARM64, OrderForce-Intelligence, runner-1]
    
    steps:
      - name: Check out the repository to the runner
        uses: actions/checkout@v4
      
      - name: Configure git
        run: |
          git config --global user.name "github-actions[OrderForce-Intelligence-bot]"
          git config --global user.email "github-actions[OrderForce-Intelligence-bot]@users.noreply.github.com"
      
      - name: Run script gen-stars-list
        run: echo "OrderForce-Intelligence" | ./gen-stars-list.sh
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
      
      - name: Commit changes
        run: |
          cd ..
          git add .
          git commit -m "Update Stars List for OrderForce-Intelligence" || exit 0
      
      - name: Push changes
        run: git push
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
